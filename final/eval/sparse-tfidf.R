library(sqldf)
drv <- dbDriver("SQLite")
con <- dbConnect(drv, dbname = "tfidf-d80-t2.5-indexed-tfidf-desc.db")
res <- dbSendQuery(con, "SELECT * FROM inverted_index LIMIT 100000")
data1 <- fetch(res, n=-1)
library(Matrix)
Y <- sparseMatrix(data1[,1], data1[,2], x=data1[,3], index1=FALSE)
library(irlba)
isvd <- irlba(Y, 1000,5)
